export default class Note {
  constructor(id, title, content, textcolor, bgcolor) {
    (this.id = id),
      (this.title = title),
      (this.content = content),
      (this.textcolor = textcolor),
      (this.bgcolor = bgcolor);
  }
}
